# LLM API Keys
OPENAI_API_KEY=your-openai-api-key
# Optional: point to a local OpenAI-compatible server (e.g., LM Studio, vLLM, Ollama w/ OpenAI endpoint)
# OPENAI_BASE_URL=http://localhost:1234/v1
# Hint Dexter about the compatibility layer (openai or ollama). Defaults to auto-detect.
# OPENAI_COMPAT_PROVIDER=openai
# Model to use (required when using a local server)
# OPENAI_MODEL=llama3.1
# Optional: use a specialized model for tool calling/argument optimization
# FUNCTION_MODEL=functiongemma
# Optional: Ollama context window for the function model
# FUNCTION_NUM_CTX=4096
# Optional tuning
# OPENAI_TIMEOUT=30
# OPENAI_MAX_RETRIES=3
# Optional: Ollama context window (OpenAI-compatible extra_body options)
# OPENAI_NUM_CTX=8192
# Optional: max parallel tool calls
# DEXTER_MAX_PARALLEL_TOOL_CALLS=4
# Optional: max characters per prompt/system prompt (hard truncate)
# DEXTER_MAX_PROMPT_CHARS=20000

# Stock Market API Key
FINANCIAL_DATASETS_API_KEY=your-financial-datasets-api-key
# Optional: override FinancialDatasets base URL (defaults to https://api.financialdatasets.ai)
# FINANCIAL_DATASETS_BASE_URL=https://api.financialdatasets.ai
